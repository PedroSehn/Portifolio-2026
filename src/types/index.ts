export interface Tech {
  name: string
  level: number
}

export interface Project {
  id: string
  num: string
  title: string
  desc: string
  tags: string[]
  color: string
  demo: string
  repo: string
}
